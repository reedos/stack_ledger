# Explorer feedback implementation

Implemented the owner-requested denser map, customizable capability colors and complete-cohort capex totals/forecasts in the existing delivery, expansion and ledger catalogs. No parallel research database, automatic approval, new publication permission or local-model run was introduced.

- 46 sourced project/program additions; 109 total and 96 mapped, covering all five layers. All old project IDs and facts retained. The source registry includes original operator/developer disclosures; location points use the reviewed Census/GeoNames datasets. The Google regional Lowcountry roll-up is deliberately excluded. Dense geographic markers now group by zoom and expose every constituent record.
- ECI country metadata restored from the exact retained source archive. Existing scores, attribution, dates, uncertainty and hash are unchanged. Default developer coloring uses the 4+ model threshold; searchable names, clickable multi-group legend, country/availability coloring, show/hide-all and reset provide alternate views.
- Decimal sums use the same five covered companies. Complete historical totals and current 2026 outlook appear on the plot. Twenty additional company forecasts reproduce a separate September 2025 consensus vintage through 2029. The later 2026 guidance is never presented as part of that older vintage. No complete 2030 annual forecast is asserted.

The Apollo chart was inspected visually: exhibit 8 uses September 2025 data, while the adjacent exhibit uses November. The 2029 Google and Microsoft labels are 159 and 153 billion respectively. Newer J.P. Morgan aggregate commentary was not mixed into this global series because its scope/caption and numerical granularity differ. The remaining gap is a newer comparable full-cohort annual forecast through 2030.

Validation uses the normal ledger/build checks, Python tests, both offline evaluators and browser acceptance. Tests cover missing-member totals, vintage separation, country metadata, developer-size defaults, group/search resets, expanded project counts, map overlap interactions and mobile layouts. Existing runtime/footer and publication-boundary fixtures remain unchanged. Private source bodies and review screenshots are excluded from Git.

Completed checks: `python scripts/validate.py`; `python scripts/build.py` (123 pages); `python -m unittest discover -s tests` (240 tests); `python scripts/evaluate_editorial.py`; `python scripts/evaluate_catalog.py`; `node tests/explorers.cjs`; `node tests/browser.cjs` (24 routes, three viewports); and `git diff --check`. Both evaluations ran offline without model inference or actual publication. Comparison against the preceding commit confirms existing observations, metric definitions, project records, runtime/run/event data, page templates and the rendered homepage footer are unchanged.
