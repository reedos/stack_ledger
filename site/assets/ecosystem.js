'use strict';
let ecosystem;
const companyOf = id => ecosystem.companies.find(c => c.id === id);
const latestRevenue = c => records(c.revenue_metric).filter(o => o.status === 'observation').at(-1);
const companyLink = id => `<a href="${base}companies/${esc(id)}/">${esc(companyOf(id).name)}</a>`;
const researchStamp = () => `<p class="chart-footnote">Company roles, projects and jobs reviewed ${dateLabel(ecosystem.reviewed_at)}. Revenue and capacity observations retain their own periods and access dates. ${ecosystem.companies.length} companies in a reviewed snapshot, not a complete industry census. Companies can serve multiple layers; counts overlap.</p>`;

function companyCard(c) {
 const o=latestRevenue(c), m=metricOf(c.revenue_metric), accent=layerOf(c.layers[0]).color;
 return `<article class="company-card" id="${esc(c.id)}" style="--accent:${accent}">
  <div class="company-heading"><span class="company-monogram" aria-hidden="true">${esc(c.name.replace('NextEra','NE').split(/\s+/).map(s=>s[0]).join('').slice(0,2))}</span><h3>${companyLink(c.id)}</h3></div>
  <div class="company-layers">${c.layers.map(id=>`<a href="${base}${id}/" style="--tag:${layerOf(id).color}">${layerOf(id).name}</a>`).join('')}</div>
  <p class="company-role">${esc(c.role)}</p>
  ${c.role_sources?`<details class="company-role-sources"><summary>Sources for company role</summary><div>${c.role_sources.map(id=>`${sourceLink(id)} · ${dateLabel(sourceOf(id).published)}`).join('<br>')}</div></details>`:''}
  ${c.revenue_kind==='unavailable'?`<div class="company-revenue unavailable"><span class="eyebrow">REVENUE COVERAGE GAP</span><strong>Not yet verified</strong><p>No sourced revenue record in this edition. This is not zero revenue; funding and valuation are not substitutes.</p></div><div class="company-source">${sourceLink(c.source)}<span>Company role source · ${dateLabel(sourceOf(c.source).published)}</span></div>`:`<div class="company-revenue ${c.revenue_kind==='run-rate'?'run-rate':''}"><span class="eyebrow">${c.revenue_kind==='run-rate'?'ANNUALIZED RUN RATE':'REPORTED ANNUAL REVENUE'}</span><strong>${esc(valueOf(o))}<small>${esc(m.unit)}</small></strong><p>${esc(o.period)}</p></div>
  <p class="company-scope">${esc(m.scope)}</p><p class="chart-footnote">${esc(o.note || m.note)}</p>
  <div class="company-source">${sourceLink(o.source)}<span>Published ${dateLabel(sourceOf(o.source).published)} · Accessed ${dateLabel(o.retrieved_at)} · ${o.method==='automated'?'Automated':'Curated'}</span></div>`}
 <a class="section-link company-profile-link" href="${base}companies/${esc(c.id)}/">Revenue history & company research ↗</a>${companyMeasures(c)}</article>`;
}

// Reviewed functional groups: order reflects the role in this layer, not revenue or rank.
const builderGroups={
 energy:[['Generation developers & operators','nextera constellation fervo tva'],['Grid delivery & system operation','mlgw ercot'],['Electrical equipment & grid construction','ge-vernova siemens-energy schneider eaton quanta'],['Solar manufacturing','first-solar qcells']],
 chips:[['Compute designers & processor IP','nvidia amd intel arm aws alphabet'],['Foundries, memory & packaging','tsmc samsung sk-hynix micron amkor'],['Chip-design tools & fabrication equipment','synopsys cadence asml applied-materials lam-research'],['Networking silicon, DSPs & electrical links','broadcom marvell credo astera cisco ciena semtech macom'],['Photonics, lasers & optical I/O','coherent lumentum applied-opto lightmatter ayar'],['Specialized compute & proposed manufacturing','tesla spacex']],
 infrastructure:[['Cloud operators & compute providers','microsoft aws alphabet meta oracle coreweave nebius alibaba spacexai'],['Campus developers & capital partners','crusoe vantage softbank'],['Servers, racks & system integration','nvidia dell supermicro foxconn quanta-computer wiwynn celestica'],['Networking silicon & switching systems','arista cisco broadcom marvell credo astera ciena'],['Optics, fiber & copper connectivity','coherent lumentum applied-opto innolight eoptolink fabrinet corning amphenol lightmatter ayar semtech macom'],['Power delivery, cooling & building systems','vertiv schneider eaton nvent coolit johnson-controls'],['Construction & electrical trades','quanta emcor turner mortenson'],['Specialized compute campuses','tesla spacex']],
 models:[['Model developers','anthropic openai spacexai alphabet meta deepseek alibaba mistral cohere ibm moonshot minimax z-ai nvidia microsoft aws ai2'],['Open model collaboration & tooling','hugging-face'],['Agent tools & runtimes','cognition cursor openclaw']],
 applications:[['Software development & digital work','anthropic openai cognition cursor openclaw microsoft'],['Enterprise operations & decision support','salesforce palantir'],['Consumer products & discovery','alphabet meta'],['Autonomous driving & physical robotics','waymo tesla agility nvidia'],['Open AI tools & applications','hugging-face'],['Drug discovery & clinical development','recursion isomorphic moderna merck']]
};
const modelContribution={nvidia:'Develops Nemotron and Cosmos world models; Omniverse provides complementary simulation tools for physical AI.',anthropic:'Builds Claude models for language, reasoning and coding.',openai:'Builds GPT and reasoning models; offers them through APIs and products.',spacexai:'Builds the Grok model family.',alphabet:'Google develops Gemini models; Google Cloud hosting is a separate contribution.',meta:'Develops Llama and Muse model families.',deepseek:'Develops DeepSeek models and publishes weights with model-specific licenses.',alibaba:'Develops Qwen models; cloud infrastructure is a separate contribution.',microsoft:'Develops MAI and Phi models. Hosting other labs’ models does not make Microsoft their developer.',aws:'Amazon develops Nova models. Bedrock also distributes models built by other companies.'};
function companySection(layer) {
 const cs=ecosystem.companies.filter(c=>c.layers.includes(layer)),seen=new Set();
 const groups=(builderGroups[layer]||[]).map(([title,ids])=>({title,companies:ids.split(' ').map(companyOf).filter(c=>c&&c.layers.includes(layer)&&!seen.has(c.id)&&(seen.add(c.id),true))}));
 const remaining=cs.filter(c=>!seen.has(c.id));if(remaining.length)groups.push({title:'Additional covered contributors',companies:remaining});
 const card=c=>`<article class="builder-card" data-company="${esc(c.id)}"><h4>${companyLink(c.id)}</h4><p>${esc(layer==='models'?(modelContribution[c.id]||c.role):c.role)}</p><div class="builder-links"><a href="${base}companies/${esc(c.id)}/">Company data & revenue ↗</a><details><summary>Role evidence</summary>${(c.role_sources||[c.source]).map(sourceLink).join(' · ')}</details></div></article>`;
 return `<section class="section" id="companies"><div class="section-top"><div><div class="eyebrow muted">THE BUILDERS · ${cs.length} COVERED CONTRIBUTORS</div><h2>Who makes this layer possible?</h2></div><a class="section-link" href="${base}companies/?layer=${layer}">Open company directory ↗</a></div><p class="section-intro">${layer==='models'?'Start with the organizations that develop the models. Agent tools and runtimes have a separate role: they connect models to context, tools and work.':'Explore the builders by what they contribute, from core producers to the suppliers and operators that make delivery possible.'} All covered contributors appear below; a company may participate in several layers. These are role groupings, not a ranking or a claim of a supplier relationship.</p><nav class="builder-nav" aria-label="Contributor roles">${groups.filter(g=>g.companies.length).map((g,i)=>`<a href="#builders-${layer}-${i}">${esc(g.title)} <span class="builder-count">${g.companies.length} contributors</span></a>`).join('')}</nav>${groups.filter(g=>g.companies.length).map((g,i)=>`<section class="builder-group" id="builders-${layer}-${i}"><h3>${esc(g.title)} <span class="builder-count">${g.companies.length} contributors</span></h3><div class="builder-grid">${g.companies.map(card).join('')}</div></section>`).join('')}<p class="chart-footnote">${cs.length} covered organizations, including companies, utilities and open-source projects where relevant. Coverage is expanding and is not an exhaustive industry census. Company pages retain source links, revenue scope and explicit data gaps.</p></section>`;
}

function companiesPage() {
 root.innerHTML=`<section class="page-hero ecosystem-hero"><div class="eyebrow">THE COMPANIES BEHIND THE STACK</div><h1>Meet the builders.</h1><p>From electricity to useful intelligence: who contributes, what they provide, and the businesses taking shape across five connected layers.</p>${researchStamp()}</section>
 <div class="ecosystem-map" aria-label="Explore companies by layer">${data.layers.map(l=>`<a href="?layer=${l.id}" data-company-layer="${l.id}" style="--accent:${l.color}"><span class="eyebrow">${l.number}</span><strong>${l.name}</strong><span>${ecosystem.companies.filter(c=>c.layers.includes(l.id)).length} companies <b aria-hidden="true">↗</b></span></a>`).join('')}</div>
 <aside class="reading-note"><strong>Read revenue in context.</strong><p>Annual revenue, segment revenue and annualized run rates measure different things. Fiscal years differ; currencies stay as reported. A company can serve several layers, and suppliers sell to each other. Adding these revenues would double-count the supply chain and would not measure AI’s contribution to GDP.</p></aside>
 <div class="directory-tools"><label for="company-search" class="sr-only">Search companies or capabilities</label><input type="search" class="search" id="company-search" placeholder="Search companies or capabilities: memory, turbines, Claude…"><label for="revenue-basis">Revenue basis <select class="select-control" id="revenue-basis"><option value="all">All disclosures</option><option value="annual">Annual revenue only</option><option value="run-rate">Run rates only</option><option value="unavailable">Revenue not yet verified</option></select></label></div>
 <div class="filters" role="group" aria-label="Filter companies by layer"><button class="filter active" data-company-filter="all" aria-pressed="true">All layers</button>${data.layers.map(l=>`<button class="filter" data-company-filter="${l.id}" aria-pressed="false">${l.name}</button>`).join('')}</div><p class="chart-footnote" id="company-count" aria-live="polite"></p><div id="company-results" class="company-grid"></div>
 <section class="manifesto"><div><div class="eyebrow muted">FOLLOW THE PHYSICAL BUILDOUT</div><h2>From business growth to real-world capacity.</h2><p>Explore operating factories, production plans and evidence on jobs.</p></div><a class="button" href="${base}industry/">Jobs & industry ↗</a></section><p class="chart-footnote"><a class="source-inline" href="${base}data/ecosystem.json">Company and industry metadata ↓</a> · Financial observations are available in the <a class="source-inline" href="${base}ledger/">ledger and CSV export</a>.</p>`;
 let selected=new URLSearchParams(location.search).get('layer') || 'all';
 if(!layerOf(selected))selected='all';
 const update=()=>{
  const q=document.querySelector('#company-search').value.trim().toLowerCase(),basis=document.querySelector('#revenue-basis').value;
  const cs=ecosystem.companies.filter(c=>(selected==='all'||c.layers.includes(selected))&&(basis==='all'||c.revenue_kind===basis)&&`${c.name} ${c.role}`.toLowerCase().includes(q));
  document.querySelector('#company-results').innerHTML=cs.length?cs.map(companyCard).join(''):'<p class="empty">No companies match these filters.</p>';
  document.querySelector('#company-count').textContent=`${cs.length} of ${ecosystem.companies.length} companies · No revenue totals across layers`;
  document.querySelectorAll('[data-company-filter]').forEach(b=>{b.classList.toggle('active',b.dataset.companyFilter===selected);b.setAttribute('aria-pressed',String(b.dataset.companyFilter===selected));});
 };
 const choose=id=>{selected=id;history.replaceState(null,'',`${location.pathname}${id==='all'?'':`?layer=${id}`}`);update();};
 document.querySelectorAll('[data-company-filter]').forEach(b=>b.addEventListener('click',()=>choose(b.dataset.companyFilter)));
 document.querySelectorAll('[data-company-layer]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();choose(a.dataset.companyLayer);document.querySelector('#company-search').scrollIntoView({block:'center'});}));
 document.querySelector('#company-search').addEventListener('input',update);
 document.querySelector('#revenue-basis').addEventListener('change',update);update();
}

function supplyChain() {
 return `<section class="section" id="supply-chain"><div class="section-top"><div><div class="eyebrow muted">INSIDE THE CHIPS LAYER</div><h2>A design becomes a device.</h2></div></div><p class="section-intro">These are distinct capabilities in a connected supply chain. Memory and equipment support fabrication and assembly; they are not simply additional steps after the chip is finished.</p><div class="supply-chain">${ecosystem.supply_chain.map((s,i)=>`<article class="supply-step"><span class="step-number">0${i+1}</span><h3>${esc(s.title)}</h3><p>${esc(s.role)}</p><div class="supplier-links">${s.companies.map(companyLink).join('')}</div><p class="chart-footnote">${esc(s.note)}</p></article>`).join('')}</div><p class="chart-footnote">Representative companies, with source-linked profiles. Additional design IP, EDA, materials, assembly and test suppliers remain a coverage priority.</p></section>`;
}

function capacitySection() {
 return `<section class="section" id="capacity"><div class="section-top"><div><div class="eyebrow muted">CAPACITY / NOW & NEXT</div><h2>More silicon. More ways to measure it.</h2></div></div><p class="section-intro">The latest measured baseline and each forecast stay separate. TSMC’s annual equivalent-wafer capacity, global advanced-node monthly capacity and global memory capacity have different scopes and cannot be added.</p><div class="capacity-grid">${ecosystem.capacity_metrics.map(id=>`<article class="panel">${chart(id)}</article>`).join('')}<aside class="panel capacity-explainer"><div class="eyebrow">WHAT WAFERS DON’T TELL US</div><h3>Delivered compute depends on the whole system.</h3><p>Process mix, die size, yield, HBM, packaging and networking determine how many working accelerators can ship. Power, cooling and utilization determine how much useful compute they deliver.</p><p>There is no defensible conversion from these wafer figures to a single global count of AI GPUs. The CoWoS series adds attributed packaging estimates. HBM stack output and domestic packaging throughput still need direct evidence.</p><a class="section-link" href="${base}industry/#projects">See factory milestones ↗</a></aside></div></section>`;
}

function industryPage() {
 const e=ecosystem.employment,max=Math.max(...e.series.map(s=>Math.abs(s.value)));
 root.innerHTML=`<section class="page-hero ecosystem-hero"><div class="eyebrow">JOBS, FACTORIES & THE LONG VIEW</div><h1>Intelligence has<br>a physical footprint.</h1><p>Reindustrialization means building lasting productive capacity. Follow power projects, chip factories and AI infrastructure into local supplier activity, skilled work and stronger communities—and test that ambition against what is actually delivered.</p>${researchStamp()}</section>
 <aside class="industry-verdict"><span class="eyebrow">OUR READING OF THE EVIDENCE</span><h2>Industrial rebuilding is visible.</h2><p>${esc(ecosystem.interpretation)}</p><p class="chart-footnote">Stack Ledger interpretation, based on the project disclosures and BLS release below. Company plans retain company attribution.</p></aside>
 ${supplyChain()}${capacitySection()}
 ${factoryMilestones()}
 <section class="section" id="jobs"><div class="section-top"><div><div class="eyebrow muted">WORK & OPPORTUNITY</div><h2>Additional workforce disclosures.</h2></div></div><p class="section-intro">Selected historical disclosures below supplement the project comparison above. They do not cover every tracked project and are not a current hiring census. Each keeps its own date and definition.</p><div class="jobs-grid">${ecosystem.jobs.map(j=>`<article class="job-card"><span class="eyebrow muted">${esc(j.status)}</span><strong class="jobs-value">${esc(valueOf(j))}</strong><h3>${esc(j.title)}</h3><p>${esc(j.period)}</p><p class="chart-footnote">${esc(j.scope)}</p>${sourceLink(j.source)}</article>`).join('')}</div><aside class="reading-note"><strong>No combined “AI jobs” total.</strong><p>Direct operating jobs, peak construction workers and modeled regional impacts overlap and have different time horizons. Announced positions are not verified hires. Net job creation also requires evidence on displacement and jobs that would have existed without these projects.</p></aside></section>
 <section class="section"><div class="section-top"><div><div class="eyebrow muted">THE WIDER LABOR MARKET</div><h2>Growth is uneven across sectors.</h2></div></div><div class="employment-layout"><figure class="panel employment-chart"><figcaption><h3>U.S. payroll change · ${esc(e.period)}</h3><p class="chart-footnote">${esc(e.basis)}</p></figcaption><div class="employment-scale"><span>Decrease</span><span>0</span><span>Increase</span></div>${e.series.map(s=>`<div class="employment-row"><div><strong>${esc(s.name)}</strong><span>${s.value>0?'+':''}${number(s.value)} jobs</span></div><div class="diverging-track" aria-hidden="true"><i class="${s.value<0?'negative':'positive'}" style="width:${Math.abs(s.value)/max*50}%"></i></div></div>`).join('')}<details class="data-details"><summary>View data table</summary><div class="table-scroll"><table><thead><tr><th>Sector</th><th>Monthly change (jobs)</th><th>Period</th></tr></thead><tbody>${e.series.map(s=>`<tr><td>${esc(s.name)}</td><td>${s.value>0?'+':''}${number(s.value)}</td><td>${esc(e.period)}</td></tr>`).join('')}</tbody></table></div></details><p class="chart-footnote">${sourceLink(e.source)} · Published ${dateLabel(sourceOf(e.source).published)}. ${esc(e.note)}</p></figure><aside class="panel"><h3>What would establish a lasting shift?</h3><p class="section-intro">Track sustained manufacturing output, domestic value added, supplier activity and realized employment across several years. Pair investment plans with commissioning and utilization.</p><ul class="research-focus">${ecosystem.gaps.map(g=>`<li>${esc(g)}</li>`).join('')}</ul></aside></div></section>`;
}

function extendWithEcosystem() {
 if(page==='home'){
  const section=`<section class="ecosystem-invitation"><div><span class="eyebrow">MEET THE BUILDERS</span><h2>Who’s building the next era?</h2><p>${ecosystem.companies.length} companies. Five connected layers. Explore their contributions, reported revenue, factory capacity and jobs.</p></div><div><a class="button" href="${base}companies/">Explore companies ↗</a><a class="button outline" href="${base}industry/">Jobs & industry ↗</a></div></section>`;
  const target=root.querySelector('.manifesto');if(target)target.insertAdjacentHTML('beforebegin',section);else root.insertAdjacentHTML('beforeend',section);
 } else if(layerOf(page)) {
  root.insertAdjacentHTML('beforeend',companySection(page)+(page==='chips'?supplyChain()+capacitySection():''));
 }
}


function companyPage() {
 const id=root.querySelector('[data-company]')?.dataset.company || location.pathname.split('/').filter(Boolean).at(-1),c=companyOf(id);
 if(!c)throw new Error('Unknown company profile');
 const mid=c.revenue_chart_metric||c.revenue_metric,m=metricOf(mid),f=m?.chart_companion_metric?metricOf(m.chart_companion_metric):null;
 const financials=m?[m,...(f?[f]:[])]:[],os=financials.flatMap(m=>records(m.id)),past=os.filter(o=>['observation','estimate'].includes(o.status)),future=os.filter(o=>o.status==='forecast');
 const years=rows=>rows.length?`${Math.min(...rows.map(o=>o.year))}–${Math.max(...rows.map(o=>o.year))}`:'Not verified';
 const extra=data.metrics.filter(x=>x.company===id&&!financials.some(m=>m.id===x.id)&&x.id!==c.revenue_metric&&records(x.id).length);
 const projects=[...new Set([...extra.map(x=>x.project).filter(Boolean),...delivery.projects.filter(p=>(p.company_ids||[]).includes(id)).map(p=>p.id)])].map(pid=>delivery.projects.find(p=>p.id===pid)).filter(Boolean);
 const peers=ecosystem.companies.filter(x=>x.id!==id&&x.layers.some(l=>c.layers.includes(l))).slice(0,8);
 root.innerHTML=`<section class="page-hero company-hero" style="--accent:${layerOf(c.layers[0]).color}"><a class="section-link" href="${base}companies/">← All companies</a><div class="eyebrow">THE BUILDERS / COMPANY RESEARCH</div><h1>${esc(c.name)}</h1><p>${esc(c.role)}</p><div class="company-layers">${c.layers.map(l=>`<a href="${base}${l}/" style="--tag:${layerOf(l).color}">${esc(layerOf(l).name)}</a>`).join('')}</div><p class="chart-footnote">Role evidence: ${(c.role_sources||[c.source]).map(sourceLink).join(' · ')}${c.ir_url?` · <a class="source-inline" href="${safeUrl(c.ir_url)}">Investor relations ↗</a>`:''}</p></section>
 <div class="company-coverage"><div><span>REPORTED HISTORY</span><strong>${years(past)}</strong></div><div><span>REVIEWED OUTLOOK</span><strong>${years(future)}</strong></div><div><span>REVENUE BASIS</span><strong>${esc(c.revenue_kind==='run-rate'?'Annualized run rate':m?.unit||'Coverage gap')}</strong></div></div>
 <section class="section company-financials" id="revenue"><div class="section-top"><div><div class="eyebrow muted">THE BUSINESS BEHIND THE BUILDOUT</div><h2>Revenue, through time.</h2></div></div>${m?`<div class="panel">${chart(mid)}</div>`:`<div class="panel"><h3>Revenue history not yet verified</h3><p>${id==='openclaw'?'OpenClaw is an open-source project. A conventional company revenue chart is not applicable without a defined reporting entity.':'No reviewed standalone revenue series is available in this edition.'}</p><p>Missing values are not zero. Funding, valuation, contracts and a parent company’s sales do not replace this entity’s revenue.</p></div>`}
 ${!future.length?`<p class="next-evidence"><strong>Forecast coverage:</strong> No comparable annual revenue forecast has been verified for this entity. ${id==='aws'?'AWS is an Amazon segment; Amazon-wide forecasts cannot stand in for AWS.':''} The page will extend when a source supplies a defined period, currency and revenue basis.</p>`:`<p class="next-evidence"><strong>Forecast horizon:</strong> ${Math.max(...future.map(o=>o.year))} is the furthest reviewed revenue year. Later paywalled estimates are not reproduced; no values are extrapolated to 2030 or beyond. A dated company scenario can have a different vintage from current analyst consensus.</p>`}
 ${c.revenue_kind==='run-rate'?'<aside class="reading-note"><strong>Run rate is not annual revenue.</strong><p>This disclosure annualizes a point-in-time business pace. It does not establish recognized revenue for the fiscal year.</p></aside>':''}
 ${c.revenue_chart_metric&&c.revenue_metric?`<details class="panel"><summary>Original reported currency series</summary>${chart(c.revenue_metric)}</details>`:''}
 <p class="chart-footnote">Company-wide revenue includes every business reported within its scope. It is not automatically AI revenue, customer productivity, local spending or jobs. Fiscal calendars and currencies differ. Public consensus is attributed to Stock Analysis / S&P Global; company outlooks name their issuer. Every point retains its access date and source.</p></section>
 <section class="section"><h2>Capital, jobs & delivered capacity.</h2>${companyMeasures(c)}${projects.length?`<div class="supplier-links">${projects.map(p=>`<a href="${base}projects/#project-${esc(p.id)}">${esc(p.name)} ↗</a>`).join('')}</div>`:''}${extra.length?`<div class="company-data-grid">${extra.map(x=>`<div class="panel">${chart(x.id)}</div>`).join('')}</div>`:'<p class="empty">Additional company-specific operating metrics remain a research priority.</p>'}</section>
 <section class="section"><h2>Connected builders.</h2><div class="supplier-links">${peers.map(c=>companyLink(c.id)).join('')}</div><p class="chart-footnote">Other covered organizations in the same layers; shared coverage does not assert a commercial partnership.</p></section>`;
}
