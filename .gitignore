.local/
.local*
__pycache__/
*.pyc
.env
node_modules/
test-results/
# Agent worktrees live under .claude/worktrees/ inside the repository, and publish() refuses to
# commit while anything untracked is present (2026-09-11).
.claude/
# Keys belong in .local/api-keys.json, already ignored above. This catches a vault written to the
# wrong directory, which is the only way one of them could reach a commit (2026-09-11).
api-keys.json
